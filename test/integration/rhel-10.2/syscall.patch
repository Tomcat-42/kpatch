diff -Nupr src.orig/kernel/sys.c src/kernel/sys.c
--- src.orig/kernel/sys.c	2026-05-14 10:41:44.133332939 -0300
+++ src/kernel/sys.c	2026-05-14 10:42:06.709081754 -0300
@@ -1315,13 +1315,15 @@ static int override_release(char __user
 	return ret;
 }
 
-SYSCALL_DEFINE1(newuname, struct new_utsname __user *, name)
+#include "kpatch-syscall.h"
+KPATCH_SYSCALL_DEFINE1(newuname, struct new_utsname __user *, name)
 {
 	struct new_utsname tmp;
 
 	down_read(&uts_sem);
 	memcpy(&tmp, utsname(), sizeof(tmp));
 	up_read(&uts_sem);
+	strcat(tmp.sysname, ".kpatch");
 	if (copy_to_user(name, &tmp, sizeof(tmp)))
 		return -EFAULT;
 
